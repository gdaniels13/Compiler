#include "logger.h"

int main()
{
	//Logger::SetLevel(ERROR);
	Logger::LogMessage("found","blah",1);
	/*
	Logger::logError("i'm an error", 1);
	Logger::logMessage("im a message", 2);
	Logger::logDebug("I'm a debug", 3);*/

}